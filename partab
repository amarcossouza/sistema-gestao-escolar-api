                <MenuItem value="Noite">Noite</MenuItem>
              </Select>
            </FormControl>
          </Grid>
          <Grid item xs={12} sm={6} md={3}>
            <FormControl fullWidth>
              <InputLabel>Turma</InputLabel>
              <Select value={turmaId} onChange={(e) => setTurmaId(e.target.value as number)} label="Turma">
                <MenuItem value="">Selecione uma turma</MenuItem>
                {turmas.map(turma => (<MenuItem key={turma.id} value={turma.id}>{turma.nome}</MenuItem>))}
              </Select>
            </FormControl>
          </Grid>
          <Grid item xs={12} sm={6} md={3}>
            <FormControl fullWidth>
              <InputLabel>Mês</InputLabel>
              <Select value={mes} onChange={(e) => setMes(e.target.value as number)} label="Mês">
                {meses.map((m, i) => (<MenuItem key={i + 1} value={i + 1}>{m}</MenuItem>))}
              </Select>
            </FormControl>
          </Grid>
          <Grid item xs={12} sm={6} md={3}>
            <FormControl fullWidth>
              <InputLabel>Ano</InputLabel>
              <Select value={ano} onChange={(e) => setAno(e.target.value as number)} label="Ano">
                {[2025, 2026, 2027, 2028, 2029, 2030].map(y => (<MenuItem key={y} value={y}>{y}</MenuItem>))}
              </Select>
            </FormControl>
          </Grid>
        </Grid>
      </Paper>
      {turmaId && alunos.length > 0 && (
        <TableContainer component={Paper} sx={{ overflowX: 'auto' }}>
          <Table size="small">
            <TableHead>
              <TableRow sx={{ backgroundColor: '#f5f5f5' }}>
                <TableCell sx={{ fontWeight: 'bold', minWidth: 50 }}>Nº</TableCell>
                <TableCell sx={{ fontWeight: 'bold', minWidth: 200 }}>Nome</TableCell>
                <TableCell sx={{ fontWeight: 'bold', textAlign: 'center', minWidth: 60 }}>Faltas</TableCell>
                {[...Array(diasDoMes)].map((_, i) => {
                  const dia = i + 1;
                  const desbloqueado = diasDesbloqueados.has(dia);
                  return (
                    <TableCell key={dia} sx={{ textAlign: 'center', padding: '8px 4px', minWidth: 45, backgroundColor: desbloqueado ? '#e3f2fd' : '#fafafa', borderLeft: '1px solid #e0e0e0', }}>
                      <Box sx={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 0.5 }}>
                        <input type="checkbox" checked={desbloqueado} onChange={() => toggleDia(dia)} style={{ cursor: 'pointer', width: 16, height: 16 }} />
                        <Typography sx={{ fontSize: '0.75rem', fontWeight: 'bold' }}>{dia}</Typography>
                        <Typography sx={{ fontSize: '0.65rem', color: '#666' }}>{getDiaDaSemana(dia)}</Typography>
                      </Box>
                    </TableCell>
                  );
                })}
              </TableRow>
            </TableHead>
            <TableBody>
              {alunos.map((aluno, idx) => (
                <TableRow key={aluno.id} sx={{ '&:hover': { backgroundColor: '#f9f9f9' } }}>
                  <TableCell sx={{ fontWeight: '500' }}>{idx + 1}</TableCell>
                  <TableCell sx={{ fontSize: '0.95rem' }}>{aluno.nome}</TableCell>
                  <Tab
